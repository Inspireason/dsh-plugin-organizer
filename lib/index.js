/** Host loader entry for the plugin-organizer browser implementation exported from `./client`. */
/** Host plugin body — no host-side behavior for the grouped inventory tab. */
function apply() {}
export { apply };
